include: package:flutter_lints/flutter.yaml

analyzer:
  exclude:
    - lib/generated_plugin_registrant.dart
    - '**/*.g.dart' # Exclude from lint, autogenerated files
    - 'lib/l10n/generated/**'
linter:
  rules:
    close_sinks: true
    cancel_subscriptions: true
